app.service('popover', ['$compile', '$templateCache', function ($compile, $templateCache) {
  var defaultOptions = {
    targetElement: null,
    template: null,
    templateUrl: null,
    scope: null
  };
  
}]);